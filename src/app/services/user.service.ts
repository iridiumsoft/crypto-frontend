import {Injectable} from "@angular/core";import {User} from "../interfaces/user.interface";@Injectable()export class UserService {    static storageName = 'user';    static tokenName = 'token';    static isLoggedIn(): boolean {        return !!this.getUser()    }    // JSON = JavaScript Object Node    static setUser(user, token: string): void {        localStorage.setItem(this.storageName, JSON.stringify(user));        localStorage.setItem(this.tokenName, token);    }    static getToken() {        return localStorage.getItem(this.tokenName);    }    static getUser(): User {        let userObject: User;        let user = localStorage.getItem(this.storageName);        if (user) {            try {                userObject = JSON.parse(user);            } catch (error) {                console.error(error.message);            }        }        return userObject    }}